{{- if .Values.ingress.enabled }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
  {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
  labels:
    heritage: {{ .Release.Service | quote }}
    release: {{ .Release.Name | quote }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    component: "{{ .Release.Name }}-web"
  name: "{{ .Release.Name }}-web"
spec:
  rules:
    - host: "airflow-{{ .Values.enclave }}.{{ .Values.rootDomain }}"
      http:
        paths:
          - backend:
              serviceName: "{{ .Release.Name }}-web"
              servicePort: 8080

    {{- if .Values.addLocalDomain }}
    - host: "{{ .Values.ingress.domain.partial }}.local.{{ .Values.rootDomain }}"
      http:
        paths:
          - backend:
              serviceName: "{{ .Release.Name }}-web"
              servicePort: 8080
    {{- end }}
    {{- if .Values.ingress.flowerEnabled}}
    - host: "flower-{{ .Values.enclave }}.{{ .Values.rootDomain }}"
      http:
        paths:
          - backend:
              serviceName: "{{ .Release.Name }}-flower"
              servicePort: 5555

    {{- if .Values.addLocalDomain }}
    - host: "{{ .Values.ingress.domain.flowerPartial }}.local.{{ .Values.rootDomain }}"
      http:
        paths:
          - backend:
              serviceName: "{{ .Release.Name }}-flower"
              servicePort: 5555
    {{- end }}
    {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
  - hosts:
    - "airflow-{{ .Values.enclave }}.{{ .Values.rootDomain }}"
    {{- if .Values.ingress.flowerEnabled}}
    - "flower-{{ .Values.enclave }}.{{ .Values.rootDomain }}"
    {{- end }}
    {{- if .Values.ingress.tls.autoSecretNameDeduction }}
    secretName: "{{ .Values.enclave }}.{{ .Values.rootDomain }}-tls"
    {{- else }}
    secretName: {{ .Values.tls.secretName  }}
    {{- end }}
  {{- end }}
{{- end }}